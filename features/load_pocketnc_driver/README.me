# load_pocketnc_driver

Old versions of the Pocket NC have an old version of cape-universal that isn't able
to properly configure the pins using config-pin. Instead, load our own driver that
configures all the pins that we need.
